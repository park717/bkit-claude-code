# Scenario Matrix Schema v1.0
# Auto-generated by PDCA Design phase, reviewed by developer
# Location: docs/02-design/scenarios/{module}.scenarios.yaml

version: "1.0"
module: ""          # Module name (e.g., "user-auth")
file: ""            # Target source file path
scenarios:
  - id: ""          # Unique ID (e.g., "S001")
    name: ""        # Human-readable scenario name
    description: "" # What this scenario tests
    input: {}       # Input parameters
    expected:
      status: 200   # Expected HTTP status or return value
      body: {}      # Expected response body
    why: ""         # Why this behavior exists (design intent)
    constraint: ""  # What must NOT change (guardrail)
    tags: []        # Tags for filtering (e.g., ["auth", "edge-case"])
    test_command: "" # Optional: specific test command to verify
